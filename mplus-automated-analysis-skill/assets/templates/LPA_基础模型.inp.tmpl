TITLE: LPA {{CLASS_COUNT}} class model generated from source-validated template LPA-BASE-EV0;

DATA:
  FILE = data.dat;

VARIABLE:
  NAMES ARE {{ALL_VARIABLES}};
  USEVARIABLES ARE {{INDICATORS}};
  IDVARIABLE = ROWID;
  MISSING = ALL({{MISSING_CODE}});
  CLASSES = C({{CLASS_COUNT}});

ANALYSIS:
  TYPE = MIXTURE;
  ESTIMATOR = MLR;
{{STARTS_BLOCK}}{{OPTSEED_BLOCK}}{{LRTSTARTS_BLOCK}}

OUTPUT:
  TECH1 TECH8{{LRT_OUTPUT}};

{{SAVEDATA_BLOCK}}
